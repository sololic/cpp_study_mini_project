C++ 설치

```bash
sudo apt update
sudo apt install g++ git python3-pip -y
```
or
```bash
sudo apt update && sudo apt install g++ -y
```

### 여기서 g++? 
C++ => 프로그래밍 언어 자체
g++ => C++ 코드를 실행파일로 변환해주는 컴파일러

